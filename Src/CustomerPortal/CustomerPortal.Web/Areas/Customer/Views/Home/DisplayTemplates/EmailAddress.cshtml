@{
    var value = ViewData.TemplateInfo.FormattedModelValue;
}

@if (string.IsNullOrWhiteSpace(Convert.ToString(value)))
{
    @Html.ValueNotSet()
    ;
}
else
{
    <a href="mailto:@Html.AttributeEncode(Model)">@value</a>
}